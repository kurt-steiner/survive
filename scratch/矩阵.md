向量映射时
$$
f(input) = output \\
A(m \times n) x(n\times 1) = b(m\times 1)
$$

$input$ 的范围有

1. $Nul(A)$
2. $Row(A)$

$output$ 的范围有

1. $Nul(A^T)$
2. $Col(A)$

其中
$$
\begin{cases}
r(Row(A)) = r(Col(A)) \\
r(Nul(A)) + r(Row(A)) = n \\
r(Nul(A^T)) + r(Col(A)) = m
\end{cases}
$$